import React from "react";
import ReactDOM from "react-dom/client";
import Expression from "./App.jsx";

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(
  <React.StrictMode>
    <Expression />
  </React.StrictMode>
);

