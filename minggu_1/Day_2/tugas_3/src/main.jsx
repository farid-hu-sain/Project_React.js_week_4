 import React from "react";
import ReactDOM from "react-dom/client";
import ConditionalDemo from "./App.jsx"; 

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(
  <React.StrictMode>
    <ConditionalDemo />
  </React.StrictMode>
);
